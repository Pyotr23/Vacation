<Window x:Class="FrontendWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrontendWPF"
        mc:Ignorable="d"
        Title="MainWindow" SizeToContent="WidthAndHeight">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="85"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="ФИО:" Margin="5" HorizontalAlignment="Right"/>
                <TextBox Grid.Column="1" Margin="5" Text="{Binding CurrentEmployee.Name, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Цвет:" Grid.Row="1" Margin="5" HorizontalAlignment="Right"/>
                <ComboBox Grid.Column="1" Grid.Row="1" Margin="5" Width="80" HorizontalAlignment="Left" ItemsSource="{Binding Colors}" SelectedItem="{Binding CurrentEmployee.Color}"/>
                <Button Content="Добавить" Grid.Column="2" Width="75" Margin="5"/>
                <Button Content="Удалить" Grid.Column="2" Grid.Row="1" Width="75" Margin="5"/>
            </Grid>
            
        </Grid>
        <DataGrid ItemsSource="{Binding Table, UpdateSourceTrigger=PropertyChanged}" CanUserAddRows="False" HorizontalAlignment="Left" Grid.Row="1"
                  SelectedItem="{Binding CurrentRow, Mode=OneWayToSource}"/>

    </Grid>
</Window>
